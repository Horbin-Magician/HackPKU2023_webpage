{
  "envId": "{{env.ENV_ID}}",
  "version": "2.0",
  "framework": {
    "name": "Github_Actions_CD",
    "plugins": {
      "client": {
        "use": "@cloudbase/framework-plugin-website",
        "inputs": {
          "installCommand": "yarn",
          "buildCommand": "yarn build",
          "outputPath": "dist",
          "cloudPath": "/"
        }
      }
    }
  }
}
